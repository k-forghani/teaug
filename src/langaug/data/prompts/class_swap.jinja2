Rewrite Persian sentences to match target sentiments. Understand the full sentiment space.

Sentiment classes:
{% for label in all_labels -%}
- {{ label.value }}
{% endfor %}

Rewrite these sentences:
{% for item in items -%}
Sentence: "{{ item.text }}"
From: {{ item.source_label }} → To: {{ item.target_label }}
{% endfor %}
Output: JSON array with rewritten sentences in order.
